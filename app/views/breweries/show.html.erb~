<p id="notice"><%= notice %></p>

<h2>
  <strong>Name:</strong>
  <%= @brewery.name %>
</h2>

<p>
  <i>
    Established in:
    <%= @brewery.year %>
  </i>
</p>

<p>
  <strong># of beers:</strong>
  <%= @brewery.beers.count %>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
      <%= link_to "#{beer.name}", beer %>
  <% end %>
</p>

<p>
<% if @brewery.ratings.empty? %>
    This brewery has no ratings yet
<% else %>
    <%= "Rated #{pluralize(@brewery.ratings.count, "times")}, average #{@brewery.average_rating}" %>
</p>


<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
